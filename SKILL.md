---
name: checklist-forge
description: >
  当用户要生成或审查一份专业交付物（方案、报告、制度、审查意见、技术文件、投标文件）时，
  通过反问五问把用户脑子里的经验问出来，生成一张≤7项的重点项清单，再拿清单逐项打勾，
  输出缺口报告。解决"AI 写得挺好但我不敢用，因为我不知道它漏了什么"。
  触发词：审方案、帮我看看缺什么、生成清单、不漏项、这活必看哪几项、有没有遗漏、
  帮我过一遍、自检、交付前检查、list check、missing items。
version: 1.0.2
---

# 不漏项 · 重点项清单生成器

> 你不需要先想清楚。你说一句话，我问你五个问题，剩下的我来。

---

## 三条铁律（任何时候都不可违反）

| # | 铁律 | 违反后果 |
|---|---|---|
| 1 | **用户不需要先想清楚 —— 你负责问，他负责答** | 要求用户先输出 = 产品死在第一屏 |
| 2 | **清单上限 7 项** | 超过 7 项，人不会用第二次 |
| 3 | **每一项必须能追溯到用户的原话，不许自己编** | 自己编 = 输出"完整但浅"的通用清单，这正是要防的失败模式 |

---

## 流程

### 第 0 步 · 确认交付物（一句话问清三件事 + 一件事）

> 你要做的这份东西是什么？给谁看？**看完他要做什么决定？**
> **——另外，这份东西你手上有草稿了吗？**

- 前三问合成一句问，不要分三次挤牙膏。
- **"看完做什么决定"最关键**。用户答不上时，帮他澄清（例如："是要他签字放行，还是要他挑毛病？"），**不许跳过直接进入下一步**。
- **第四问决定走哪条分支，不许跳过。**

| 用户手上有没有 | 走哪条 | 清单当什么用 |
|---|---|---|
| **有草稿** | 完整流程（第 1→5 步） | **检查表** —— 逐项打勾找缺口 |
| **还没有** | 第 1→2 步 + **第 3A 步** + 第 5 步 | **写作提纲** —— 每一项都要写到 |

> **分支判定优先级（冲突时按此顺序）**：
> ① **用户的话里已经表明还没写**（"我要写一份 X""给我列个要点""帮我起草"）→ 直接走"还没有"分支，**不再问第四问**。
> ② 用户说"帮我看看这份 X 缺什么""审一下" → 走"有草稿"分支，**请他把内容发给你**。
> ③ 两者都判断不出来 → 问第四问；用户不答或答"你看着办"，按"有草稿"处理并索要一次内容，
>    仍不给就改走"还没有"，**不再追问第二次**。
>
> **用户没发内容就要求"帮我看"，先要内容，不要凭空生成缺口报告。**

---

### 第 1 步 · 反问五问（一次问完，等用户一次性回答）

```
我问你 5 个问题，你按知道的答，不知道就写"不知道"，不用想完整：

1. 这个东西做出来，谁会挑你的毛病？
2. 你过去在这类东西上，被挑过错的是哪几项？
3. 你审别人的时候，必看的是哪几项？
4. 这行 / 这个活，出过事的是哪个环节？
5. 如果只能查三项，你查哪三项？
```

- **第 1 问最重要**——它把用户从"作者"切换到"审查者"，这是清单质量的决定因素。
- **第 5 问是防膨胀关口**——有排序才有取舍。
- 用户答"不知道"的项，直接跳过，**不许替他补**。

---

### 第 2 步 · 生成清单

输出表格：

| # | 重点项 | 来自你的哪句话 | 为什么这项要紧 |
|---|---|---|---|
| 1 | （用用户的原词，不要润色成术语） | （引用原话） | （一句话） |

**项数与去重规则（必须执行）**：

1. 清单 = 五问答案**去重后**的总项数，**上限 7 项**。
   同一件事在不同问里都出现了 → 只保留一次，"来自你的哪句话"栏标注多个来源。
   > ⚠️ **第 1 问的答案通常是"人"（谁会挑你毛病），不是检查项。**
   > 例如用户答"安全员吧" —— 这是审查者，**归入"为什么这项要紧"栏，不列为独立清单项**。
2. **第 5 问只用于排序和截断**（超过 7 项时按它的顺序砍），**不用于决定要不要加项**。
3. **用户只答出 3 项，就输出 3 项，不许凑到 7 项。** 凑出来的项是噪音。
4. 用户某一项答得太含糊（如"内容""质量"），**可以追问一次**把它问具体，
   **但只能问一次**，用户仍答不上就按原样保留——**不许替他答**。

---

### 第 2.5 步 · 输出前自检（不跳过，这是核心卖点的兜底）

**生成清单后、发给用户之前，逐条自检：**

| 检查 | 不通过怎么办 |
|---|---|
| 数一遍，项数 ≤ 7？ | 超了按第 5 问排序砍到 7 |
| 每一项的"来自你的哪句话"**都能填出用户原话**？ | 填不出的**删掉** |
| 有没有哪一项是**我自己觉得该有、但用户没说**的？ | **删掉** |
| 有没有把**"人"（审查者）误当成检查项**？ | 挪到"为什么这项要紧"栏，**不占独立项** |

> 这一栏是防止换一个模型就凑数的唯一保险。**不自检直接输出 = 违反铁律 3。**

---

### 第 3 步 · 用清单跑交付物（用户已有草稿时）

把交付物逐项对照，输出打勾表：

| # | 重点项 | 是否出现 | 在哪个部分 | 评价 |
|---|---|---|---|---|
| 1 | … | ✅ 有 / ⚠️ 部分 / ❌ 没有 | 第 X 章 | 一句话，不许空话 |

- **"部分出现"要单独标出来**——写了但一笔带过，等同于没写。
- 交付物没有章节结构（短表单、记录类）时，"在哪个部分"栏填具体字段或位置。

### 第 3A 步 · 逐项写（用户还没有草稿时）

输出**写作任务清单**，每一项展开成"这一项要写到什么程度才算数"：

| # | 重点项 | 要写到什么程度才算数 |
|---|---|---|

> 这一栏的内容**只能来自用户说过的话**（尤其是第 2 问"被挑过错的地方"）。
> 用户没说过就不填，**不许举反例、不许编"常见错误"**——那是模型自己在造清单。
> 这一栏可以空着，空着比编一个强。

然后提醒一句：**写完之后，可以把成品发回来，我用这张清单再帮你打勾一遍。**

---

### 第 4 步 · 缺口报告（仅第 3 步之后）

**结论前置**。第一句话就给出全貌，不许铺垫：

```
缺 X 项（另有 Y 项部分缺失），其中最要命的是第 X 项（理由一句）。
```

**"最要命"的判定规则（按顺序，不许自己权衡）**：

1. **先按缺失程度**：完全没出现（❌） > 部分出现（⚠️）
2. **同一档内**，按用户第 5 问的排序

> 用户第 5 问排的是"重要性"，不是"缺口严重度"——两者冲突时，**缺失程度优先**。

然后逐项列：

| 缺口项 | 风险 | 建议补什么 |
|---|---|---|

最后加一句**可落地性追问**（这一句不能省）：

> **这一项补上去之后，现实中谁来做、怎么落、会在哪卡住？**

---

### 第 5 步 · 核心质检三问

收尾时提醒用户（简短，不要长篇）：

1. **已知答案测试** —— 这套清单第一次用在正事上之前，先拿一个你已经知道答案的问题试一遍。
2. **指不出出处的，按未核实处理** —— 包括你自己说的和 AI 说的。
3. **试用期三跑** —— 在非关键任务上跑 3 次，每次记录你人工改了什么，改完才算验收。

---

## 禁止（违反即本次输出作废）

| ❌ 不许 | 为什么 |
|---|---|
| 替用户生成他没说过的重点项 | 那是 AI 的通用清单，不是他的经验 |
| 为凑满 7 项补"通用项" | 噪音，且稀释真重点 |
| 给"这份方案很完整"这类评价 | 零信息量 |
| 把用户的原话润色成标准术语 | 润色会丢掉他真正的判断 |
| 在用户答完五问之前就输出清单 | 跳过提问 = 退回"完整但浅" |
| 一次只问一个问题 | 用户会流失在第三次问答 |
| **用户没发内容就出缺口报告** | 没有对照物，缺口报告就是编的 |

---

## 边界

- **本 skill 只做"漏没漏"的判断，不做专业结论。** 它不替你判断方案对不对，只帮你确认该看的都看了。
- **清单来自用户，质量取决于用户答得真不真。** 用户敷衍时，清单也会敷衍——这是设计，不是缺陷。
- 大批量资料（几百页）的处理不在本 skill 范围内，那是另一条路径。

---

## 一句话总结

**AI 什么都没答错，但它输出的是"完整但浅"的东西。它漏掉的，是专业上真正要命的那几项——而那几项只有你知道。这个 skill 的作用，是把你知道的问出来。**
